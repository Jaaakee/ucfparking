name: "garages_training"

on:
  # Triggers the workflow every 1 minute
  schedule:
    - cron: "*/1 * * * *"
jobs:
  training:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Run a one-line script
      run: python ./api/predictions/training/train_garage_models.py
    - name: switching from HTTPS to SSH
      run: git remote set-url origin git@github.com:jakeva/ucfparking.git
    - name: check for changes
      run: git status
    - name: stage changed files
      run: git add .
    - name: commit changed files
      run: git commit -m "Upload the updated trained models for each garage."
    - name: fetch from master
      run: git fetch origin master
    - name: push code to master
      run: git push origin HEAD:master